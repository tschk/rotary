--- src/tools/common.rs
+++ src/tools/common.rs
@@ -263,6 +263,7 @@
     }

     #[test]
+    #[cfg(feature = "computer-use")]
     fn test_resolve_write_path() {
         let dir = tempfile::tempdir().unwrap();
         let ws = dir.path();
